{
  "name": "socialocean",
  "private": true,
  "version": "0.0.0",
  "description": "Social Ocean - Mouth to Mouth branding",
  "repository": "https://github.com/dgaeta/socialocean/",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1",
    "main-bower-files": "^2.8.0"
  },
  "scripts": {
    "postinstall": "bower install --config.directory='app/lib'",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1",
    "pretest": "npm install",
    "preupdate-webdriver": "npm install"
  },
  "dependencies": {
    "del": "^1.2.0"
  }
}
